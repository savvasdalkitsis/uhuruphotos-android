dependencies {
    implementation project(":initializer")
    implementation project(":icons")
    implementation(
            libs.androidx.core,
            libs.androidx.work,
            libs.hilt,
    )
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.notification'
}