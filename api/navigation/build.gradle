android {
    buildFeatures {
        compose true
    }
    composeOptions {
        kotlinCompilerExtensionVersion compose_version
    }
    namespace 'com.savvasdalkitsis.uhuruphotos.api.navigation'
}

dependencies {

    api project(':api:seam')
    implementation project(":api:log")
    implementation project(":api:toaster")
    implementation project(":api:ui")
    implementation project(":api:strings")
    implementation(
            libs.accompanist.systemui,
            libs.androidx.compose.foundation,
    )
    api(
            libs.accompanist.navigation,
            libs.androidx.compose.hilt.navigation,
            libs.hilt,
    )
}