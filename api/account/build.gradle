android {
    namespace 'com.savvasdalkitsis.uhuruphotos.api.account'
}

dependencies {
    implementation(
            libs.androidx.compose.material,
    )
    androidTestImplementation(
            libs.bundles.test.android,
    )
    testImplementation(
            libs.bundles.test,
    )
}