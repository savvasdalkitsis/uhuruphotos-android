android {
    buildFeatures {
        compose true
    }
    composeOptions {
        kotlinCompilerExtensionVersion compose_version
    }
    namespace 'com.savvasdalkitsis.uhuruphotos.api.image'
}

dependencies {

    implementation(
            libs.coil.compose,
            libs.coil.gif,
            libs.coil.video,
    )
}