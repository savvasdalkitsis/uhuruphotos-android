dependencies {
    api(
            libs.androidx.compose.foundation,
            libs.exoplayer.base,
    )
    implementation(
            libs.hilt,
    )
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.api.video'
}