plugins {
    id "org.jetbrains.kotlin.multiplatform"
}

kotlin {
    jvm {
        compilations.configureEach {
            kotlinOptions {
                jvmTarget = '17'
            }
        }
    }
}