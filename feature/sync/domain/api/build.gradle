plugins {
    id "android.library"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.sync.domain.api'
}

dependencies {
    api(
            libs.kotlinx.coroutines.android,
            project(":feature:upload:domain:api"),
    )
}