plugins {
    id "android.library"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.download.domain.api'
}

dependencies {
    api(
            libs.kotlinx.coroutines,
            project(":feature:media:common:domain:api"),
    )
    implementation(
            project(":foundation:log:api"),
    )
}