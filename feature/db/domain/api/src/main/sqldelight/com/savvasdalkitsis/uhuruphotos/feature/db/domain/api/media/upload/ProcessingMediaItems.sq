CREATE TABLE processingMediaItems (
    id INTEGER NOT NULL PRIMARY KEY
);

insert:
INSERT OR REPLACE INTO processingMediaItems VALUES (:id);

delete:
DELETE FROM processingMediaItems WHERE id = :id;

getAll:
SELECT id FROM processingMediaItems;