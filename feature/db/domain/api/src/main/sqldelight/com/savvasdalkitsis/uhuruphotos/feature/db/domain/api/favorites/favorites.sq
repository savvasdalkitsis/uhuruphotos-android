CREATE TABLE favorites (
    md5sum TEXT NOT NULL PRIMARY KEY
);

isFavorite:
SELECT EXISTS(SELECT 1 FROM favorites WHERE md5sum = :md5sum);

markFavorite:
INSERT OR REPLACE INTO favorites VALUES (:md5sum);

unmarkFavorite:
DELETE FROM favorites WHERE md5sum = :md5sum;