plugins {
    id "android.library"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.upload.domain.api'
}

dependencies {
    api(
            libs.kotlinx.immutable,
            project(":feature:media:common:domain:api"),
    )
    implementation project(":foundation:strings:api")
}