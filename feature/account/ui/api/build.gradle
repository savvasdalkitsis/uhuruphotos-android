android {
    buildFeatures {
        compose true
    }
    composeOptions {
        kotlinCompilerExtensionVersion compose_compiler_version
    }
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.account.ui.api'
}

dependencies {
    implementation(
            libs.androidx.compose.material,
    )
    androidTestImplementation(
            libs.bundles.test.android,
    )
    testImplementation(
            libs.bundles.test,
    )
}