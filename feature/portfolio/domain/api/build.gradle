plugins {
    id "android.library"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.portfolio.domain.api'
}

dependencies {
    api(
            libs.kotlinx.coroutines.android,
            project(":feature:media:common:domain:api"),
    )
}