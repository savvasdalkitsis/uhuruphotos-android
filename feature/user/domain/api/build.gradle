android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.user.domain.api'
}

dependencies {
    api(
            project(":feature:db:domain:api"),
            project(":foundation:result:api"),
            libs.kotlinx.coroutines,
    )
}