plugins {
    id "android.library"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.uploads.domain.api'
}

dependencies {
    api(
            libs.kotlinx.coroutines,
            project(":feature:upload:domain:api"),
    )
}