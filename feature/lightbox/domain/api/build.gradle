plugins {
    id "android.library"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.lightbox.domain.api'
}

dependencies {
    api(
            libs.kotlinx.coroutines.android,
            project(":feature:db:domain:api"),
            project(":feature:media:common:domain:api"),
    )
}