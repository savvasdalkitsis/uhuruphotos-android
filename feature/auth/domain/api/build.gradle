android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.auth.domain.api'
}

dependencies {
    api(
            libs.kotlinx.coroutines,
    )
    implementation(
            libs.kotlinx.coroutines,
            libs.hilt,
    )
}