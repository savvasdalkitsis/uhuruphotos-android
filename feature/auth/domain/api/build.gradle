android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.auth.domain.api'
}

dependencies {
    api(
            project(":foundation:result:api"),
            libs.androidx.compose.foundation,
    )
    implementation(
            libs.hilt,
    )
}