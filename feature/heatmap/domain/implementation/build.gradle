plugins {
    id "android.library"
    }

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.heatmap.domain.implementation'
}

kotlin {
    sourceSets {
        commonMain {
            dependencies {
                implementation project(":feature:heatmap:domain:api")
                implementation project(":foundation:preferences:api")
            }
        }
    }
}
