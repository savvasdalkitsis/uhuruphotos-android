plugins {
    id "android.library"
    }

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.heatmap.domain.implementation'
}

dependencies {
    implementation(
            project(":feature:heatmap:domain:api"),
            project(":foundation:preferences:api"),
    )
}