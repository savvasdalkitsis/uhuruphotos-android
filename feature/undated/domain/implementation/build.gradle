android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.undated.domain.implementation'
}

dependencies {
    implementation(
            libs.bundles.autodagger,
            libs.hilt,
            project(":feature:undated:domain:api"),
            project(":foundation:preferences:api"),
    )
    ksp libs.autodagger.ksp
}