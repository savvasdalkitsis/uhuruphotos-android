android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.catalogue.user.domain.api'
}

dependencies {
    api(
            project(":feature:catalogue:view:api"),
            project(":feature:catalogue:user:view:api"),
            project(":foundation:result:api"),
            libs.kotlinx.coroutines,
    )
}