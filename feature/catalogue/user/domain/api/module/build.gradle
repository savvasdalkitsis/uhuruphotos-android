plugins {
    id "android.library"
    id "ktorfit"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.catalogue.user.domain.api.module'
}

kotlin {
    sourceSets {
        commonMain {
            dependencies {
                api project(":feature:catalogue:user:domain:api")
                implementation project(":feature:auth:domain:api:module")
                implementation project(":feature:db:domain:api:module")
                implementation project(":feature:catalogue:user:domain:implementation")
                implementation project(":foundation:inject:api")
                implementation project(":foundation:preferences:api:module")
            }
        }
    }
}