plugins {
    id "android.library"
    id "hilt"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.memories.domain.implementation'
}

dependencies {
    implementation(
            project(":feature:feed:domain:api"),
            project(":feature:memories:domain:api"),
            project(":foundation:date:api"),
    )
}