plugins {
    id "android.library"
    alias(libs.plugins.kotlinx.serialization)
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.feature.site.domain.api'
}
dependencies {
    implementation(
            libs.kotlinx.serialization,
    )
}