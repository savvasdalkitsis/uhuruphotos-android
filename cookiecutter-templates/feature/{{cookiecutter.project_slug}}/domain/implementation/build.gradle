android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.{{cookiecutter.project_slug}}.domain.implementation'
}

dependencies {
    implementation project(":feature:{{cookiecutter.project_slug}}:domain:api")
    implementation(
            libs.autodagger.core,
    )
    kapt libs.autodagger.kapt
}