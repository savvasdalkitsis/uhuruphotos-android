android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.{{cookiecutter.project_slug}}.implementation'
}

dependencies {
    implementation(
            libs.hilt,
            project(":foundation:{{cookiecutter.project_slug}}:api"),
    )
}