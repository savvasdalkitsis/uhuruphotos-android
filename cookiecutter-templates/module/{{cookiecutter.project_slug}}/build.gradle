plugins {
    id "android.library"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.{{cookiecutter.project_slug}}.api.module'
}

dependencies {
    api project(":foundation:{{cookiecutter.project_slug}}:api")
    implementation project(":foundation:inject:api")
    implementation project(":foundation:{{cookiecutter.project_slug}}:implementation")
}