plugins {
    id "android.library"
    id "kotlin-parcelize"
    alias(libs.plugins.compose)
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.navigation.api'
}

kotlin {
    sourceSets {
        commonMain {
            dependencies {
                api libs.appyx.backstack
                api libs.appyx.navigation
                api libs.bundles.viewmodel
                api project(':foundation:initializer:api')
                api project(":foundation:log:api")
                api project(":foundation:seam:api")
                api project(":foundation:theme:api")
            }
        }
    }
}

dependencies {
    api(
            libs.androidx.compose.navigation,
            libs.bundles.resaca,
            libs.kotlin.reflect,
            libs.appyx.backstack,
            libs.appyx.navigation,
    )
}