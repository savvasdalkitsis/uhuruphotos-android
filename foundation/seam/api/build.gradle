android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.seam.api'
}

dependencies {

    implementation(
            libs.androidx.lifecycle,
            libs.kotlinx.coroutines,
            project(":foundation:launchers:api"),
            project(":foundation:log:api"),
    )
}