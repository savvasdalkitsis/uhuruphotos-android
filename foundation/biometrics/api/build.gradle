plugins {
    alias(libs.plugins.kotlin.multiplatform)
}

kotlin {
    jvm("desktop")
    sourceSets {
        commonMain {
            dependencies {
                api(project(":foundation:result:api"))
            }
        }
    }
}