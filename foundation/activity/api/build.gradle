plugins {
    id "android.library"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.activity.api'
}

dependencies {
    api(
            libs.androidx.activity,
            project(":foundation:result:api"),
    )
    implementation(
            libs.hilt,
    )
}