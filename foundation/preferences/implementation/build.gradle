plugins {
    id "android.library"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.preferences.implementation'
}

dependencies {
    implementation(
            libs.androidx.datastore.preferences,
            libs.androidx.preference,
            libs.bundles.androidx.security.crypto,
            libs.bundles.autodagger,
            libs.flowpreferences,
            libs.hilt,
            project(":foundation:preferences:api"),
    )
    ksp libs.autodagger.ksp
}