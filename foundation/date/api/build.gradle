plugins {
    id "android.library"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.date.api'
}

dependencies {

    api(
            libs.joda.time,
    )
    implementation(
            libs.hilt,
            project(":foundation:log:api"),
            project(":foundation:strings:api"),
    )
}