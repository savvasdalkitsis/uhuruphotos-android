plugins {
    alias(libs.plugins.kotlin.multiplatform)
}

kotlin {
    jvm("desktop")
    androidTarget()
    sourceSets {
        commonMain {
            dependencies {
                api(libs.kotlinx.coroutines)
                implementation project(":foundation:log:api")
            }
        }
    }
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.coroutines.api'
}