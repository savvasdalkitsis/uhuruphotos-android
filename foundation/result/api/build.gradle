android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.result.api'
}

sourceSets {
    commonMain {
        dependencies {
            api(
                    libs.kotlinx.coroutines,
                    libs.bundles.result,
            )
        }
    }
}