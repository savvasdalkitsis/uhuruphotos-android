plugins {
    id "android.library"
}

android {
    namespace "com.savvasdalkitsis.uhuruphotos.foundation.toaster.api.module"
}

kotlin {
    sourceSets {
        commonMain {
            dependencies {
                api(project(":foundation:toaster:api"))
                implementation(project(":foundation:toaster:implementation"))
            }
        }
    }
}

dependencies {
    implementation(project(":foundation:android:api:module"))
}