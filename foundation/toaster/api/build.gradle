android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.toaster.api'
}

kotlin {
    jvm("desktop")
    androidTarget()
    sourceSets {
        commonMain {
            dependencies {
                api(project(":foundation:strings:api"))
            }
        }
    }
}