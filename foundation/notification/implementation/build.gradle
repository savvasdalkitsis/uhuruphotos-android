plugins {
    id "android.library"
    }

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.notification.implementation'
}

dependencies {
    implementation(
            libs.androidx.core,
            libs.androidx.work,
            project(":foundation:icons:api"),
            project(":foundation:initializer:api"),
            project(":foundation:notification:api"),
            project(":foundation:strings:api"),
    )
}