plugins {
    id "android.library"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.network.api'
}

dependencies {
    api(
            libs.autodagger.core,
            libs.autodagger.ktorfit,
            libs.okhttp,
            libs.kotlinx.serialization,
            libs.ktorfit.core,
    )
    implementation(
            libs.hilt,
    )
}