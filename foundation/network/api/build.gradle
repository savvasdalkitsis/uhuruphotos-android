plugins {
    id "android.library"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.network.api'
}

dependencies {
    api(
            libs.okhttp,
            libs.kotlinx.serialization,
            libs.ktorfit.core,
    )
}