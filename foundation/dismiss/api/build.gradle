plugins {
    alias(libs.plugins.kotlin.compose.compiler)
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.dismiss.api'
}

dependencies {

    implementation(
            libs.androidx.core,
            libs.androidx.compose.animation.base,
            libs.androidx.compose.foundation,
            libs.androidx.compose.ui,
    )
}