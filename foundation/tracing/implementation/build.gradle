plugins {
    id "android.library"
    id "hilt"
}

android {
    namespace 'com.savvasdalkitsis.uhuruphotos.foundation.tracing.implementation'
}

dependencies {
    implementation(
            libs.androidx.tracing,
            project(":foundation:initializer:api"),
    )
}