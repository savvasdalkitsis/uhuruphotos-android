android {
    buildFeatures {
        compose true
    }
    composeOptions {
        kotlinCompilerExtensionVersion compose_version
    }
    namespace 'com.savvasdalkitsis.uhuruphotos.image.api'
}

dependencies {

    implementation(
            libs.coil.compose,
            libs.coil.gif,
            libs.coil.video,
    )
}