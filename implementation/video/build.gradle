android {
    buildFeatures {
        compose true
    }
    composeOptions {
        kotlinCompilerExtensionVersion compose_compiler_version
    }
    namespace 'com.savvasdalkitsis.uhuruphotos.implementation.video'
}

dependencies {

    implementation project(":api:auth")
    implementation project(":api:settings")
    implementation project(':foundation:video:api')
    implementation(
            libs.androidx.compose.foundation,
            libs.androidx.compose.material,
            libs.androidx.compose.ui,
            libs.exoplayer.okhttp,
            libs.hilt,
            libs.exoplayer.base,
    )
}