android {
    buildFeatures {
        compose true
    }
    composeOptions {
        kotlinCompilerExtensionVersion compose_version
    }
    namespace 'com.savvasdalkitsis.uhuruphotos.implementation.image'
}

dependencies {

    implementation project(":api:auth")
    implementation project(':api:image')
    implementation project(":api:initializer")
    implementation project(":api:settings")
    implementation(
            libs.coil.compose,
            libs.coil.gif,
            libs.coil.video,
            libs.hilt,
    )
}