android {
    buildFeatures {
        compose true
    }
    composeOptions {
        kotlinCompilerExtensionVersion compose_compiler_version
    }
    namespace 'com.savvasdalkitsis.uhuruphotos.implementation.account'
}

dependencies {

    implementation project(":api:account")
    implementation project(":api:db")
    implementation project(":foundation:image:api")
    implementation project(':foundation:video:api')
    implementation project(":foundation:worker:api")
    implementation(
            libs.androidx.compose.foundation,
            libs.androidx.compose.material,
            libs.okhttp,
            libs.hilt,
    )
    testImplementation(
            libs.bundles.test,
    )
}